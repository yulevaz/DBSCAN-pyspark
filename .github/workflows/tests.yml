name: DBSCAN-PYSPARK

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: chrsep/alpine-java-python@master
      - id: reqs
        run: pip install -r /home/runner/work/DBSCAN-pyspark/requirements.txt
      - id: tests
        run: pytest tests_DBSCAN.py
